{
  "name": "@context-broker/adapter-memory",
  "version": "0.1.0",
  "description": "Memory adapter for durable architectural decisions, session notes, and verified project facts",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@context-broker/contracts": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
